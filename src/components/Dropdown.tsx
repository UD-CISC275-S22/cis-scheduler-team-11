import React, { useState } from "react";
import { Button, Container, Row, Col, Form } from "react-bootstrap";
import { ButtonData } from "../interfaces/projectInterfaces";

export function DropdownMenu(buttons: ButtonData[]): JSX.Element {
    /*const [display]*/
    return (
        <div>
            <div>●●●</div>
        </div>
    );
}
